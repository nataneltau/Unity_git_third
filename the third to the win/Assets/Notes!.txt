1. In goblin attack right animation there are no function calls for the "AttackInAnimation" and "AllowMovement" methods, but it somehow calls them
	if in the future there will be a problem that this animation don't execute those methods know why, and probably add call to them in the animation.

